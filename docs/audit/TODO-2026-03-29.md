# FLOWX 내일 TODO — 2026-03-29 (일요일)

## P0 — 반드시 먼저 (감리 보완)

### 1. 실제 데이터 검증 (모든 봇)
- [ ] `/api/quant-jarvis` 호출 → `data.cfo`, `data.cto`, `data.fundamentals` 키 존재 확인
- [ ] `/api/stock?ticker=005930` 호출 → `master`, `technicals`, `valuations`, `ml_prediction` 모두 non-null 확인
- [ ] `/api/ml-predictions` 호출 → 최신 날짜 데이터 반환 확인
- [ ] `/api/stock-master?ticker=005930` 호출 → name/sector/market 반환 확인

### 2. 브라우저 렌더링 확인
- [ ] `/quant` 성과탭 → CFO 건강카드 렌더링 (건강점수 게이지, 경고 배지)
- [ ] `/quant` 성과탭 → CTO 소스성과 바차트 렌더링
- [ ] `/quant` 펀더멘탈 탭 → 실적 상태 분포 바 + 턴어라운드 테이블
- [ ] `/stock/005930` → 종목명 + 마켓 배지 + 섹터 배지 표시
- [ ] `/stock/005930` → ML 예측 섹션 (판정/확률/기준가/팩터)

### 3. 단타봇 월요일 실전 검증 준비
- [ ] `/cfo` 텔레그램 명령어 응답 확인
- [ ] `/cto` 텔레그램 명령어 응답 확인
- [ ] CFO.record_trade() 매도 시 실제 호출 확인 (로그)

## P1 — 감리 구조 개선

### 4. 정보봇/퀀트봇 Evaluator 단계 추가
- [ ] 정보봇: ml_predictions 적중률 로직을 T+1 실제 데이터로 1회 검증
- [ ] 퀀트봇: CFO/CTO/fundamentals 데이터가 실제로 quant_jarvis에 업로드되었는지 확인
  ```sql
  SELECT date,
    data->'cfo' IS NOT NULL as has_cfo,
    data->'cto' IS NOT NULL as has_cto,
    data->'fundamentals' IS NOT NULL as has_fundamentals
  FROM quant_jarvis
  ORDER BY date DESC LIMIT 1;
  ```

## P2 — 다음 기능

### 5. 추가 작업 가능성 확인
- [ ] 정보봇/퀀트봇에 새 handoff가 있는지 확인
- [ ] OpenAI/Gemini API 키 활용 계획 논의 (현재 .env에 있지만 미사용)

## 참고: 현재 웹봇 페이지 맵

| 경로 | 컴포넌트 | 데이터 소스 |
|------|----------|------------|
| `/quant` | JarvisControlTower (8탭) | quant_jarvis |
| `/scenarios` | ScenarioDashboard | quant_scenario_dashboard |
| `/smart-money` | SmartMoneyView | dashboard_smart_money |
| `/etf-signals` | EtfSignalsView | dashboard_etf_signals |
| `/relay` | RelayView | dashboard_relay |
| `/stock/[ticker]` | StockDetailView | stock + technicals + valuations + ml_predictions + master |
| `/chart/[ticker]` | 차트 | ohlcv |

## 참고: 정보봇 갱신 스케줄

| 시간 | 테이블 |
|------|--------|
| 16:00~17:30 | quant_jarvis (퀀트봇 COO) |
| 16:54 | stock_master |
| 17:00 | stock_technicals |
| 17:05 | stock_valuations |
| 17:10 | ml_predictions 적중률 업데이트 |
| 18:35 | ml_predictions 신규 예측 |
