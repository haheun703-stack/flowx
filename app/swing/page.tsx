import { SwingTabsView } from '@/features/swing/ui/SwingTabsView'

export const metadata = {
  title: 'FLOWX — 스윙시스템 (VIP)',
  description: 'BRAIN AI 판단 + 자산배분 + 섹터수급 + 모멘텀 + ETF수급',
}

export default function SwingPage() {
  return <SwingTabsView />
}
